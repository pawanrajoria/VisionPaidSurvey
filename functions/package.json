{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "logs": "firebase functions:log",
    "build:final": "ng build && rd /s /q functions\\dist && move dist functions\\dist",
    "deploy": "npm run build:final && firebase deploy --only functions,hosting"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "express": "^4.20.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.3",
    "graphql": "^16.12.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0",
    "typescript": "^5.7.3"
  },
  "private": true
}